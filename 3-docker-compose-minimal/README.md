### Run
```
$ docker-compose up
```

## Run detached (background)
```
$ docker-compose up -d
```

## Close detached (background)
```
$ docker-compose down
```

> Refer to the `README` in `1-docker-minimal` directory